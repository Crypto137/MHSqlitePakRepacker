# MHSqlitePakRepacker

A tool for converting legacy SQLite-based Gazillion pak archives used in older versions of Marvel Heroes to the format used by later versions (1.29+).

Drag and drop `mu_cdata.sip` onto `MHSqlitePakRepacker.exe` to convert (or use the command prompt if you are a nerd).
